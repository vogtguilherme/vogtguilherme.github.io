source "https://rubygems.org"

# Matches the gem versions GitHub Pages uses in production, so local
# `bundle exec jekyll serve` behaves the same as the deployed site.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"
